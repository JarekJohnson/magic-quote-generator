import { Routes } from '@angular/router';
import { AppComponent } from './app';
/**
 * Application routes.
 * Currently default, but included for future scalability.
 */
export const routes: Routes = [{ path: '', component: AppComponent }];
